{
    "$comment": "Via",
    "required": [
        "two",
        "one"
    ],
    "type": "object",
    "properties": {
        "two": {
            "$comment": "second field",
            "type": "string"
        },
        "one": {
            "$comment": "first field",
            "type": "string"
        }
    }
}