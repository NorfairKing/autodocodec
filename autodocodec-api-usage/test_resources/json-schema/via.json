{
    "$comment": "Via",
    "required": [
        "one",
        "two"
    ],
    "type": "object",
    "properties": {
        "two": {
            "$comment": "second field",
            "type": "string"
        },
        "one": {
            "$comment": "first field",
            "type": "string"
        }
    }
}